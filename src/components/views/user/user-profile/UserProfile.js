import React from "react";

function UserProfile(props) {
  //check if the user is logged in
  //if yes, show the user info
  //if no, redirect to login
  return <div>User Profile view</div>;
}

export default UserProfile;
